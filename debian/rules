#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS=hardening=+all

%:
	dh $@

override_dh_auto_test:
ifeq (,$(filter nocheck,$(DEB_BUILD_OPTIONS)))
	$(MAKE) -C tests
endif

override_dh_auto_install:
	$(MAKE) install DESTDIR=$(CURDIR)/debian/mle/ prefix=/usr
